# Terminal file manager (Yazi)
{...}: {
  programs = {
    yazi = {
      enable = true;
      settings = {
        mgr = {
          show_hidden = true;
          sort_dir_first = true;
        };
      };
    };
  };
}
